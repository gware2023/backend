<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dev.gware.auth.mapper.AuthMapper">

    <select id="findByKey" parameterType="UsrInfo">
        SELECT USR_KEY as usrKey, USR_ID as usrId, KOR_NM as korNm, EMAIL_ADDR as emailAddr, USR_PWD as usrPwd
        FROM USR_INFO
        WHERE USR_KEY = #{usrKey}
    </select>

    <!-- 유저 조회 -->
    <select id="findById" parameterType="UsrInfo" resultType="UsrInfo">
        SELECT USR_KEY as usrKey, USR_ID as usrId, KOR_NM as korNm, EMAIL_ADDR as emailAddr, USR_PWD as usrPwd, REFRESH_TOKEN as refreshToken
        FROM USR_INFO
        WHERE USR_ID = #{usrId}
    </select>

    <!-- 리프레시 토큰 재발급 -->
    <update id="updateRefreshTokenById" parameterType="UpdateRefreshTokenRequest">
        UPDATE USR_INFO SET REFRESH_TOKEN = #{refreshToken}
        WHERE USR_KEY = #{usrKey}
    </update>
</mapper>